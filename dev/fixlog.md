# Fix log — журнал правок (Куток)

Журнал зроблених правок екранів. Сторінка `dev/fixlog.html` читає цей файл
разом із двома серверами — анотацій на `:4747` (що ще в роботі, туди
клацають прямо на макеті) і вердиктів на `:4748` (доробки, «на доробку») —
і збирає з усіх трьох одну таблицю.

## Формат

Одна секція `## РРРР-ММ-ДД` на день, під нею — Markdown-таблиця. Порядок і
назви колонок мають лишатися такими: парсер у `fixlog.html` шукає їх за
назвою заголовка (`час`, `трив`, `роут`, `запит`, `маршрут`, `зроблено`), а
не за позицією, але сама назва має містити те слово.

| Час | Трив. | Роут | Запит | Маршрут | Що зроблено |
|-----|-------|------|-------|---------|-------------|

- **Час** — `ГГ:ХХ`, коли правку взяли в роботу.
- **Трив.** — скільки тривала правка: `45с`, `3.9хв`, `2хв 10с`, `1год 5хв`,
  `<1хв`.
- **Роут** — шлях html-файла екрана, напр. `/wireframes/listings.html`. Якщо
  той самий екран, що й рядком вище, можна написати `те саме`.
- **Запит** — що просили зробити. Можна `` `code` `` і `**bold**`.
- **Маршрут** — хто зробив правку: `сам`, `agent`, `opencode`,
  `agent + impeccable`.
- **Що зроблено** — короткий опис результату.

Нові рядки дописуються в кінець поточної секції дня; перший рядок нового дня
заводить нову секцію `## РРРР-ММ-ДД` над таблицею.

## 2026-08-14

| Час | Трив. | Роут | Запит | Маршрут | Що зроблено |
|-----|-------|------|-------|---------|-------------|
| 10:20 | 8хв | `/wireframes/listings.html` | на десктопі меню має бути вгорі: лого зліва, пункти по центру, профіль справа; панель не зелена; пошук перенести в панель і показувати розгорнутим | agent | Десктопна розкладка оболонки перевернута з сайдбара зліва на верхню панель: у `base.css` grid-areas `"nav" "head" "main"` в одну колонку, у `tabbar.css` смуга стала горизонтальною на `--bg-surface` + `--border-default` (замість оливкового `--bg-navbar`), пункти на `--text-primary`, активний — `--color-brand`, «Профіль» відсунуто `margin-left:auto`. У розмітку таб-бару додано лого і розгорнуте поле пошуку (сховані на мобільному) — перенесено на всі 72 екрани + `ui/kit.html`, `ui/shell.html`, `docs/`; у `header.css` бренд-рядок і кнопка-лупа на десктопі ховаються, шапка втратила `sticky` на користь панелі. Мобільна пігулка не змінилась. |
| 10:41 | 4хв | `/wireframes/login.html` | все це по центру і не таке широке на десктопі (`main`) | agent | Заведено токен `--container-narrow: 33.75rem` (540px) у `design-system/tokens.css` — стеля для екранів-потоків, поряд з `--container-max`. У `base.css`, у гілці `@media (min-width: 67.5rem)`, новий модифікатор оболонки `.app--flow`: `.app-head` і `main` дістають `max-width: var(--container-narrow)` + `margin-inline: auto`, тож шапка і форма стоять однією вузькою колонкою по центру, а не тягнуться на 1100px. Клас проставлено на `login.html`, `login-error.html`, `login-loading.html` (три стани одного екрана). `bash design-system/components/build.sh` виконано. Мобільна і планшетна ширини не змінились — правка живе тільки в десктопній `@media`. |
| 10:42 | 3хв | `/wireframes/listings.html` | більше карток зроби (`ul.feed`) | agent | У стрічку додано 8 карток — було 4, стало 12: 8 кімнат (Лук’янівка, Нивки, Осокорки, Голосіїв, Сирець та наявні) і 4 картки-людини (Артем, Марта, Ігор, Ліза). Розмітка кожної нової `li > a.card` зібрана з тих самих класів компонента (`card-photo`, `place`, `badge badge--on-photo`, `card-body`, `card-price`), фото — з того ж пулу Unsplash, що вже вживається на інших екранах. Порядок перемішано так, щоб картки-люди не вишиковувались одним стовпчиком у 3-колонковій десктопній сітці. Це правка контенту одного екрана: жодного токена, CSS-правила компонента чи розмітки в `ui/kit.html` не чіпав, тож `build.sh` не потрібен і розсинхрону немає — сусідні стани (`listings-empty`, `-loading`, `-error`) мають власний набір карток за визначенням. 12 карток дають рівно 4 повних ряди на десктопі (3 кол.) і 6 на планшеті (2 кол.). |
| 10:38 | 6хв | `/wireframes/listings.html` | вгорі на десктопі мають бути (`fieldset.seg` — сегмент «Кімнати · Люди · Усі») | agent | Перевірка, не нова правка. Причина зауваження — grid-розкладка оболонки з правки 10:20: `grid-template-areas "nav" "head" "main"` іменувала лише три елементи, а `.filterbar` із сегментом лишалась без area й авто-розміщувалась у неявний ряд ПІСЛЯ `main`, тобто падала під стрічку. На момент цієї правки оболонку вже завернули на рев'ю назад у flex-колонку з `order: -1` на `.tabbar` (`base.css` + `tabbar.css`), і сегмент разом з рештою неіменованих секцій повернувся у порядок документа — одразу під шапку. Виконано `bash design-system/components/build.sh`: `index.css` перезібрано, діфа немає — збірка вже була в синхроні з джерелами. Перевірено headless-скріном на 1280px: смуга меню вгорі, під нею шапка «Пошук», далі сегмент, далі стрічка. Правок у токенах, компонентах чи розмітці не робив — розсинхрону немає; правка живе тільки в десктопній `@media (min-width: 67.5rem)`, мобільна й планшетна ширини не зачеплені. |
| 10:43 | 9хв | `/wireframes/listings.html` | **rework:** «меню на декктопі має бути тільки горизонтальне вгорі без фону зеленого. на мобілці і планшеті не міняй» (доробка правки 10:20) | agent | Причина завороту знайшлась у розкладці, а не в кольорі: правка 10:20 зробила `.app` на десктопі `display: grid` з `grid-template-areas: "nav" "head" "main"`. Areas іменують лише три елементи, тому інші прямі нащадки `.app` (на listings — `.filterbar` із сегментом «Кімнати · Люди · Усі», на chat — композер) авто-розміщувались у **неявні ряди після `main`** і падали на низ сторінки. Внизу з'являлась друга горизонтальна смуга-пігулка, візуально не відрізнити від меню — звідси «меню має бути **тільки** вгорі». Полагоджено зміною механіки, а не латкою: оболонка на десктопі лишається тією самою flex-колонкою, що й на телефоні, а панель піднімається `order: -1` (`tabbar.css` замість `grid-area: nav`); з `base.css` прибрано `display: grid` / `grid-template-rows` / `grid-template-areas` і `.app main { grid-area: main }`, з `header.css` — `grid-area: head` (лишилось `position: static`). Порядок розмітки тепер тримається сам, іменувати треба лише те, що справді переїжджає. Колір панелі не чіпав — вона й була `--bg-surface` + `--border-default` з 10:20. **Розсинхрон:** правка суто в правилах компонентів (`base.css`, `tabbar.css`, `header.css`), розмітку не міняв — тому нічого переносити на 72 екрани й у `ui/kit.html` не треба, а `bash design-system/components/build.sh` виконано (35 файлів). Токени не чіпав. Перевірено в headless Chrome на 390 / 820 / 1300: мобільна й планшетна ширини піксель-у-піксель ті самі (зелена пігулка внизу, сегмент угорі), на десктопі сегмент став під шапкою над стрічкою, меню — єдина горизонтальна смуга вгорі. Додатково перевірені `chat` (композер лишився внизу, шапка `--back` компактна), `candidates` і `login` (`.app--flow` з правки 10:41 працює у flex так само). |
| 10:47 | 4хв | `/wireframes/login.html` | вище має бути на декстопі шапка і лого (`header.app-head`) | agent | Побічний ефект правки 10:20: у `header.css`, у десктопній гілці, бренд-рядок ховався безумовно (`.head-brand-row { display: none }`) — на тій підставі, що лого на десктопі живе у верхній панелі меню. Але на екранах входу/реєстрації таб-бару немає взагалі, тож ховати не було куди: десктопний `login` лишався зовсім без знака бренду й слова «Куток». Правило звужено до тих оболонок, де панель справді є: `.app:has(> .tabbar) .head-brand-row` і `.app:has(> .tabbar) .app-head > .head-btn`. **Розсинхрон:** правка — в правилі компонента (`design-system/components/header.css`), не на екрані; розмітку не чіпав, тому переносити на екрани й у `ui/kit.html` нічого. `bash design-system/components/build.sh` виконано (35 файлів). Токени не чіпав. Правка живе тільки в `@media (min-width: 67.5rem)` — мобільна й планшетна ширини не зачеплені. Перевірено headless-скріном на 1280: на `login` бренд-рядок з лого видно над «Вхід», на `listings` (є таб-бар) лого лишилось лише в панелі — дублювання не з'явилось. |
| 10:48 | 8хв | `/wireframes/role-select.html` | не таке широке (`main`) | agent | Перевірка, не нова правка. Анотація знята о 10:39 (`computedStyles` показує `width: 1100px` — тобто `--container-max`), а модифікатор `.app--flow` доїхав на цей екран у ході правки 10:41→«на схожих екранах поправ», уже після зняття. На момент розбору `wireframes/role-select.html` вже має `class="app app--flow"`, а `design-system/components/base.css` (гілка `@media (min-width: 67.5rem)`) віддає `.app--flow > .app-head` і `.app--flow > main` стелю `--container-narrow` (33.75rem/540px) + `margin-inline: auto`. Перевірено в headless Chrome: на 1400px шапка «Хто ви в Кутку?» і колонка з двома `.role-option` стоять однією вузькою колонкою по центру (≈540px), а не на 1100px; на 820px (планшет) розкладка не змінилась — правило живе тільки в десктопній `@media`. **Розсинхрон:** нічого не правив — ні токени, ні `design-system/components/*.css`, ні розмітку, тому `build.sh` не потрібен і переносити на інші екрани нічого. Правило вже спільне (модифікатор оболонки в `base.css`), а не локальне для одного екрана. |
| 10:49 | 7хв | `/wireframes/consent.html` | не таке широке, на схожих екранах поправ (`main`) | agent | На десктопі `main` екрана згоди тягнувся на всі 1100px (`--container-max`) — рядок чекбокса з правовим текстом виходив далеко за комфортну довжину. Механізм для цього вже є з правки 10:41 — модифікатор оболонки `.app--flow` у `design-system/components/base.css` (гілка `@media (min-width: 67.5rem)`), що дає `.app-head` і `main` стелю `--container-narrow` (33.75rem/540px) + `margin-inline: auto`. Замість заводити нове правило — проставив цей клас на весь сімейний набір «схожих екранів», тобто на всі, чий `main` — одна колонка форми/тексту, а не каталог: `consent`, `role-select`, `verify` + `verify-phone` / `verify-code` / `verify-selfie` / `verify-pending` з усіма станами, `profile-create-seeker` / `profile-create-host` зі станами, `room-create` зі станами, `my-profile-seeker-edit`, `my-profile-host-edit`, а в архіві — `recovery`, `report` (+`-loading`/`-error`/`-sent`), `support` (+`-loading`), `privacy-settings`, `account`, `legal`. Разом 35 файлів, проставлено скриптом (`sed` по списку) і перевірено `grep`, що клас однаковий у кожному. Каталогові екрани (`listings`, `applications`, `candidates`, `dialogs`, `chat`, `profile`, `my-listings`, `my-profile-*`, `room`) свідомо лишились на `--container-max`: там ширина = більше карток, а не довший рядок. **Розсинхрон:** нового CSS-правила не з'явилось — правка це розмітка (клас-модифікатор на `.app`), тому вона й поїхала одразу на всі екрани сімейства, а не на один; у `base.css` оновлено лише коментар над правилом, щоб критерій «хто отримує `.app--flow`» був записаний біля самого правила, а не жив у голові. `bash design-system/components/build.sh` виконано (35 файлів). Токени не чіпав — `--container-narrow` заведено ще о 10:41. Правило живе тільки в десктопній `@media (min-width: 67.5rem)`: мобільна й планшетна ширини не зачеплені. Візуально headless Chrome цього разу не перевірив — на цій машині він стабільно вішається на всіх варіантах прапорців (три спроби, кожна впала в таймаут); правка спирається на вже перевірене на `login` і `role-select` правило, її ефект — одне вже відоме `max-width`. |
