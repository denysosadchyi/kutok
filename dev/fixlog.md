# Fix log — журнал правок (Куток)

Журнал зроблених правок екранів. Сторінка `dev/fixlog.html` читає цей файл
разом із двома серверами — анотацій на `:4747` (що ще в роботі, туди
клацають прямо на макеті) і вердиктів на `:4748` (доробки, «на доробку») —
і збирає з усіх трьох одну таблицю.

## Формат

Одна секція `## РРРР-ММ-ДД` на день, під нею — Markdown-таблиця. Порядок і
назви колонок мають лишатися такими: парсер у `fixlog.html` шукає їх за
назвою заголовка (`час`, `трив`, `роут`, `запит`, `маршрут`, `зроблено`), а
не за позицією, але сама назва має містити те слово.

| Час | Трив. | Роут | Запит | Маршрут | Що зроблено |
|-----|-------|------|-------|---------|-------------|

- **Час** — `ГГ:ХХ`, коли правку взяли в роботу.
- **Трив.** — скільки тривала правка: `45с`, `3.9хв`, `2хв 10с`, `1год 5хв`,
  `<1хв`.
- **Роут** — шлях html-файла екрана, напр. `/wireframes/listings.html`. Якщо
  той самий екран, що й рядком вище, можна написати `те саме`.
- **Запит** — що просили зробити. Можна `` `code` `` і `**bold**`.
- **Маршрут** — хто зробив правку: `сам`, `agent`, `opencode`,
  `agent + impeccable`.
- **Що зроблено** — короткий опис результату.

Нові рядки дописуються в кінець поточної секції дня; перший рядок нового дня
заводить нову секцію `## РРРР-ММ-ДД` над таблицею.

## 2026-08-14

| Час | Трив. | Роут | Запит | Маршрут | Що зроблено |
|-----|-------|------|-------|---------|-------------|
| 10:20 | 8хв | `/wireframes/listings.html` | на десктопі меню має бути вгорі: лого зліва, пункти по центру, профіль справа; панель не зелена; пошук перенести в панель і показувати розгорнутим | agent | Десктопна розкладка оболонки перевернута з сайдбара зліва на верхню панель: у `base.css` grid-areas `"nav" "head" "main"` в одну колонку, у `tabbar.css` смуга стала горизонтальною на `--bg-surface` + `--border-default` (замість оливкового `--bg-navbar`), пункти на `--text-primary`, активний — `--color-brand`, «Профіль» відсунуто `margin-left:auto`. У розмітку таб-бару додано лого і розгорнуте поле пошуку (сховані на мобільному) — перенесено на всі 72 екрани + `ui/kit.html`, `ui/shell.html`, `docs/`; у `header.css` бренд-рядок і кнопка-лупа на десктопі ховаються, шапка втратила `sticky` на користь панелі. Мобільна пігулка не змінилась. |
| 10:41 | 4хв | `/wireframes/login.html` | все це по центру і не таке широке на десктопі (`main`) | agent | Заведено токен `--container-narrow: 33.75rem` (540px) у `design-system/tokens.css` — стеля для екранів-потоків, поряд з `--container-max`. У `base.css`, у гілці `@media (min-width: 67.5rem)`, новий модифікатор оболонки `.app--flow`: `.app-head` і `main` дістають `max-width: var(--container-narrow)` + `margin-inline: auto`, тож шапка і форма стоять однією вузькою колонкою по центру, а не тягнуться на 1100px. Клас проставлено на `login.html`, `login-error.html`, `login-loading.html` (три стани одного екрана). `bash design-system/components/build.sh` виконано. Мобільна і планшетна ширини не змінились — правка живе тільки в десктопній `@media`. |
| 10:42 | 3хв | `/wireframes/listings.html` | більше карток зроби (`ul.feed`) | agent | У стрічку додано 8 карток — було 4, стало 12: 8 кімнат (Лук’янівка, Нивки, Осокорки, Голосіїв, Сирець та наявні) і 4 картки-людини (Артем, Марта, Ігор, Ліза). Розмітка кожної нової `li > a.card` зібрана з тих самих класів компонента (`card-photo`, `place`, `badge badge--on-photo`, `card-body`, `card-price`), фото — з того ж пулу Unsplash, що вже вживається на інших екранах. Порядок перемішано так, щоб картки-люди не вишиковувались одним стовпчиком у 3-колонковій десктопній сітці. Це правка контенту одного екрана: жодного токена, CSS-правила компонента чи розмітки в `ui/kit.html` не чіпав, тож `build.sh` не потрібен і розсинхрону немає — сусідні стани (`listings-empty`, `-loading`, `-error`) мають власний набір карток за визначенням. 12 карток дають рівно 4 повних ряди на десктопі (3 кол.) і 6 на планшеті (2 кол.). |
| 10:38 | 6хв | `/wireframes/listings.html` | вгорі на десктопі мають бути (`fieldset.seg` — сегмент «Кімнати · Люди · Усі») | agent | Перевірка, не нова правка. Причина зауваження — grid-розкладка оболонки з правки 10:20: `grid-template-areas "nav" "head" "main"` іменувала лише три елементи, а `.filterbar` із сегментом лишалась без area й авто-розміщувалась у неявний ряд ПІСЛЯ `main`, тобто падала під стрічку. На момент цієї правки оболонку вже завернули на рев'ю назад у flex-колонку з `order: -1` на `.tabbar` (`base.css` + `tabbar.css`), і сегмент разом з рештою неіменованих секцій повернувся у порядок документа — одразу під шапку. Виконано `bash design-system/components/build.sh`: `index.css` перезібрано, діфа немає — збірка вже була в синхроні з джерелами. Перевірено headless-скріном на 1280px: смуга меню вгорі, під нею шапка «Пошук», далі сегмент, далі стрічка. Правок у токенах, компонентах чи розмітці не робив — розсинхрону немає; правка живе тільки в десктопній `@media (min-width: 67.5rem)`, мобільна й планшетна ширини не зачеплені. |
| 10:43 | 9хв | `/wireframes/listings.html` | **rework:** «меню на декктопі має бути тільки горизонтальне вгорі без фону зеленого. на мобілці і планшеті не міняй» (доробка правки 10:20) | agent | Причина завороту знайшлась у розкладці, а не в кольорі: правка 10:20 зробила `.app` на десктопі `display: grid` з `grid-template-areas: "nav" "head" "main"`. Areas іменують лише три елементи, тому інші прямі нащадки `.app` (на listings — `.filterbar` із сегментом «Кімнати · Люди · Усі», на chat — композер) авто-розміщувались у **неявні ряди після `main`** і падали на низ сторінки. Внизу з'являлась друга горизонтальна смуга-пігулка, візуально не відрізнити від меню — звідси «меню має бути **тільки** вгорі». Полагоджено зміною механіки, а не латкою: оболонка на десктопі лишається тією самою flex-колонкою, що й на телефоні, а панель піднімається `order: -1` (`tabbar.css` замість `grid-area: nav`); з `base.css` прибрано `display: grid` / `grid-template-rows` / `grid-template-areas` і `.app main { grid-area: main }`, з `header.css` — `grid-area: head` (лишилось `position: static`). Порядок розмітки тепер тримається сам, іменувати треба лише те, що справді переїжджає. Колір панелі не чіпав — вона й була `--bg-surface` + `--border-default` з 10:20. **Розсинхрон:** правка суто в правилах компонентів (`base.css`, `tabbar.css`, `header.css`), розмітку не міняв — тому нічого переносити на 72 екрани й у `ui/kit.html` не треба, а `bash design-system/components/build.sh` виконано (35 файлів). Токени не чіпав. Перевірено в headless Chrome на 390 / 820 / 1300: мобільна й планшетна ширини піксель-у-піксель ті самі (зелена пігулка внизу, сегмент угорі), на десктопі сегмент став під шапкою над стрічкою, меню — єдина горизонтальна смуга вгорі. Додатково перевірені `chat` (композер лишився внизу, шапка `--back` компактна), `candidates` і `login` (`.app--flow` з правки 10:41 працює у flex так само). |
| 10:47 | 4хв | `/wireframes/login.html` | вище має бути на декстопі шапка і лого (`header.app-head`) | agent | Побічний ефект правки 10:20: у `header.css`, у десктопній гілці, бренд-рядок ховався безумовно (`.head-brand-row { display: none }`) — на тій підставі, що лого на десктопі живе у верхній панелі меню. Але на екранах входу/реєстрації таб-бару немає взагалі, тож ховати не було куди: десктопний `login` лишався зовсім без знака бренду й слова «Куток». Правило звужено до тих оболонок, де панель справді є: `.app:has(> .tabbar) .head-brand-row` і `.app:has(> .tabbar) .app-head > .head-btn`. **Розсинхрон:** правка — в правилі компонента (`design-system/components/header.css`), не на екрані; розмітку не чіпав, тому переносити на екрани й у `ui/kit.html` нічого. `bash design-system/components/build.sh` виконано (35 файлів). Токени не чіпав. Правка живе тільки в `@media (min-width: 67.5rem)` — мобільна й планшетна ширини не зачеплені. Перевірено headless-скріном на 1280: на `login` бренд-рядок з лого видно над «Вхід», на `listings` (є таб-бар) лого лишилось лише в панелі — дублювання не з'явилось. |
| 10:48 | 8хв | `/wireframes/role-select.html` | не таке широке (`main`) | agent | Перевірка, не нова правка. Анотація знята о 10:39 (`computedStyles` показує `width: 1100px` — тобто `--container-max`), а модифікатор `.app--flow` доїхав на цей екран у ході правки 10:41→«на схожих екранах поправ», уже після зняття. На момент розбору `wireframes/role-select.html` вже має `class="app app--flow"`, а `design-system/components/base.css` (гілка `@media (min-width: 67.5rem)`) віддає `.app--flow > .app-head` і `.app--flow > main` стелю `--container-narrow` (33.75rem/540px) + `margin-inline: auto`. Перевірено в headless Chrome: на 1400px шапка «Хто ви в Кутку?» і колонка з двома `.role-option` стоять однією вузькою колонкою по центру (≈540px), а не на 1100px; на 820px (планшет) розкладка не змінилась — правило живе тільки в десктопній `@media`. **Розсинхрон:** нічого не правив — ні токени, ні `design-system/components/*.css`, ні розмітку, тому `build.sh` не потрібен і переносити на інші екрани нічого. Правило вже спільне (модифікатор оболонки в `base.css`), а не локальне для одного екрана. |
| 10:49 | 7хв | `/wireframes/consent.html` | не таке широке, на схожих екранах поправ (`main`) | agent | На десктопі `main` екрана згоди тягнувся на всі 1100px (`--container-max`) — рядок чекбокса з правовим текстом виходив далеко за комфортну довжину. Механізм для цього вже є з правки 10:41 — модифікатор оболонки `.app--flow` у `design-system/components/base.css` (гілка `@media (min-width: 67.5rem)`), що дає `.app-head` і `main` стелю `--container-narrow` (33.75rem/540px) + `margin-inline: auto`. Замість заводити нове правило — проставив цей клас на весь сімейний набір «схожих екранів», тобто на всі, чий `main` — одна колонка форми/тексту, а не каталог: `consent`, `role-select`, `verify` + `verify-phone` / `verify-code` / `verify-selfie` / `verify-pending` з усіма станами, `profile-create-seeker` / `profile-create-host` зі станами, `room-create` зі станами, `my-profile-seeker-edit`, `my-profile-host-edit`, а в архіві — `recovery`, `report` (+`-loading`/`-error`/`-sent`), `support` (+`-loading`), `privacy-settings`, `account`, `legal`. Разом 35 файлів, проставлено скриптом (`sed` по списку) і перевірено `grep`, що клас однаковий у кожному. Каталогові екрани (`listings`, `applications`, `candidates`, `dialogs`, `chat`, `profile`, `my-listings`, `my-profile-*`, `room`) свідомо лишились на `--container-max`: там ширина = більше карток, а не довший рядок. **Розсинхрон:** нового CSS-правила не з'явилось — правка це розмітка (клас-модифікатор на `.app`), тому вона й поїхала одразу на всі екрани сімейства, а не на один; у `base.css` оновлено лише коментар над правилом, щоб критерій «хто отримує `.app--flow`» був записаний біля самого правила, а не жив у голові. `bash design-system/components/build.sh` виконано (35 файлів). Токени не чіпав — `--container-narrow` заведено ще о 10:41. Правило живе тільки в десктопній `@media (min-width: 67.5rem)`: мобільна й планшетна ширини не зачеплені. Візуально headless Chrome цього разу не перевірив — на цій машині він стабільно вішається на всіх варіантах прапорців (три спроби, кожна впала в таймаут); правка спирається на вже перевірене на `login` і `role-select` правило, її ефект — одне вже відоме `max-width`. |
| 10:51 | 9хв | `/wireframes/applications.html` | покращи дизайн (`.row row--muted`) | agent | Анотація знята на десктопі (`width: 1068px` у `computedStyles`) — і саме там був дефект: `.list` не мав нічого схожого на десктопну поведінку `.feed`, тож рядок розтягувався на всю стелю `--container-max`. Аватар з іменем і статусом стояли зліва, піґулка «перенесено в чат» — аж біля правого краю, між ними півекрана порожнечі: око не зшивало це в один запис. Дві правки в `design-system/components/list.css`. (1) З `--bp-tablet` `.list` став сіткою, але з `grid-template-columns: repeat(auto-fit, minmax(22rem, 1fr))`, а не фіксованими `repeat(2, …)` як у `.feed`: у рядка зміст горизонтальний (аватар + текст + піґулка), тому рахувати треба ширину самого списку, а не вікна — у навігаторі вайрфреймів ліва панель з'їдає 258px, і фіксовані дві колонки давали по ~290px з рваним переносом («Кімната на / Подолі»). Трьох колонок навмисно немає: на ~340px піґулка «очікує» налазить на ім'я. `align-items` лишено `stretch` (не `start`): роздільник живе на `<li>`, і при `start` сусідні li різної висоти давали дві лінії на різних рівнях — сітка читалась рваною. (2) `.row` дістав бокові відступи `var(--space-10)` з від'ємним `margin-inline` і `--radius-12`: підсвітка ховера й рамка фокуса тепер лягають скругленою плямою з полем навколо тексту, а не смугою впритул до літер від краю до краю; сам рядок стоїть там само, де стояв. Додано `transition` на фон з `--duration-1`/`--easing-1` — ті самі токени, що в `.seg`. **Розсинхрон:** правка суто в правилі компонента (`list.css`), розмітку не чіпав — тому переносити нічого ні на екрани, ні в `ui/kit.html`, а зміна автоматично поїхала на всі екрани з `.list` (`applications`, `dialogs`, `candidates`, `my-listings` + їхні `-loading`, `_archive/blocked`, `kit`). `bash design-system/components/build.sh` виконано (35 файлів). Токенів не заводив і не міняв — усі значення вже були (`--grid-gap`, `--space-10`, `--radius-12`, `--duration-1`, `--easing-1`); 22rem — нижня межа колонки в самому `repeat()`, не тема й не ребренд. Причину записано в `DESIGN.md` §7. Перевірено headless Chrome на 1500 / 900 / 430: на десктопі два записи в ряд з вирівняними роздільниками, на планшеті (стейдж ~640px) і на мобільному — одна колонка, розкладка та сама, що була. Регресію перевірено на `dialogs` — той самий виграш, `.row-side` з часом і лічильником лишився на своєму місці. |
| 11:15 | 5хв | `/wireframes/listings.html` | поле постав зліва профілю і не таке широке (`form.tabbar-search` у десктопній панелі) | agent | Пошук у верхній панелі стояв одразу за лого й тягнув на себе `--size-320` (320px), а вся група пунктів меню відсувалась праворуч; візуально поле читалось як частина бренд-зони, а не як інструмент поруч із «Профілем». Переставлено в праву пару: у `design-system/components/tabbar.css` (гілка `@media (min-width: 67.5rem)`) `.tabbar > .tabbar-search` дістав `order: 1`, а `.tabbar a:last-child` («Профіль») — `order: 2`, тож поле стоїть безпосередньо зліва від профілю. Auto-margin переїхав відповідно: раніше центрування трималось на `margin-right: auto` у пошуку + `margin-left: auto` у «Профілі», тепер — `margin-right: auto` на `.tabbar-brand` і `margin-left: auto` на `.tabbar-search`, тому група пунктів так само центрується в тому, що лишилось між лого і правою парою, а пара «пошук + Профіль» не розривається. Ширина зменшена з `--size-320` на `--size-220`. **Розсинхрон:** значення геометрії — у `design-system/tokens.css`, PRIMITIVE-рівень (заведено `--size-220: 220px` поряд із `--size-300`/`--size-320`; семантичного рівня для геометрії за CLAUDE.md немає й не заводимо), правило — у `design-system/components/tabbar.css`, розмітку не чіпав, тому переносити нічого ні на екрани, ні в `ui/kit.html`, а зміна автоматично поїхала на всі 60+ екранів із таб-баром. `bash design-system/components/build.sh` виконано (35 файлів). Правка живе тільки в десктопній `@media (min-width: 67.5rem)` — на мобільному й планшеті `.tabbar-search` лишається `display: none` (там пошук у поповері за лупою), розкладка пігулки не зачеплена. Перевірено headless Chrome на 1400: лого зліва, «Пошук · Заявки · Чати» по центру, далі вужче поле пошуку і «Профіль» справа. |
| 11:19 | 4хв | `/wireframes/listings.html` | відступи по вертикалі більше (`ul.feed`) | agent | Анотацію знято на десктопі (`width: 1100px`, `display: grid`, `gap: 24px`) — і дефект саме сітковий: `gap: var(--grid-gap)` задавав однакові 24px і між колонками, і між рядами. У колонках сусіда відділяє ще й порожнє поле обабіч картки, а по вертикалі картки стоять впритул одна над одною, тож при однаковому зазорі ряди злипались у суцільну стрічку без ритму. Заведено PRIMITIVE-токен `--grid-row-gap: 2.5rem` (40px) у `design-system/tokens.css`, поряд із `--grid-gap` (геометрія — тільки PRIMITIVE, семантичного рівня для неї за CLAUDE.md немає), і в `design-system/components/feed.css` зазор розділено на дві осі: `gap: var(--grid-row-gap) var(--grid-gap)`. **Розсинхрон:** значення — у токенах, правило — у компоненті `feed.css`, розмітку не чіпав, тому переносити нічого ні на екрани, ні в `ui/kit.html`, а зміна автоматично поїхала на всі екрани зі стрічкою (`listings`, `my-listings`, `profile`-фото та їхні стани). `bash design-system/components/build.sh` виконано (35 файлів). Правка живе тільки в гілці `@media (min-width: 47.5rem)`, де `.feed` взагалі стає сіткою: одноколонкова мобільна стрічка лишилась на flex із `gap: var(--space-24)` і не зачеплена. Перевірено headless Chrome на 1100: між рядами карток видимо більше повітря, ніж між колонками, ряди читаються окремо. |
| 12:19 | 1хв | `/wireframes/my-profile-seeker.html` | лого «Куток» не має отримувати кольоровий hover/background у спільному desktop footer.tabbar | agent + impeccable | У `design-system/components/tabbar.css` додано найвужчий desktop override для `.tabbar > .tabbar-brand:hover` з прозорим фоном; hover звичайних nav-пунктів лишився без змін. **Розсинхрон за CLAUDE.md:** правка лише у спільному компоненті, розмітку wireframe та токени не чіпав; `bash design-system/components/build.sh` виконано, тож `index.css` перебудовано зі спільного джерела. Перевірено selector/override grep-перевіркою; mobile/tablet правила не зачеплені. |
| 12:19 | 4хв | `/wireframes/my-profile-seeker.html` | desktop `.profile-hero` має бути скругленим і без бордера | agent + impeccable | У спільному desktop-правилі `design-system/components/profile-hero.css` прибрано фактичний `border-right`; sticky-поверхня бере наявний primitive `--radius-22`, що відповідає контейнерам системи. **Розсинхрон:** правка живе в правилі компонента, розмітку екрана й `ui/kit.html` не чіпав, отже вона автоматично однакова на всіх detail-профілях; після цього виконано `bash design-system/components/build.sh`, щоб `components/index.css` лишився синхронним із джерелом. Правило обмежене `@media (min-width: 67.5rem)`, тому mobile і tablet не змінюються. |
| 12:25 | 10хв | `/wireframes/room-create.html` | це все має під декстоп фомуватися (main) | agent | Наявна системна desktop-адаптація доведена/перевірена: `.app--edit` від tablet формує пов’язані поля у 2 колонки, `.app--preview` на desktop дає form+sticky preview; усі room-create стани мають app--edit, основний має app--preview; preview-компонент додано у ui/kit.html; правило живе у responsive @media гілках, mobile не змінено; layout.css є компонентним правилом, тому bash design-system/components/build.sh виконано (35 файлів); screenshot pass 1440/430; розсинхрону немає. |
| 12:30 | 14хв | `/wireframes/my-profile-seeker.html` | не повинно бути полів цих, бо вибраний сегмент повна адаптація (`.wf-stage`) | agent | Причина — `.app` мав `max-width: var(--container-max)` 1100px при stage 1273px; у desktop @media в `design-system/components/base.css` системно для detail-архетипу додано `.app--detail { max-width: none; }`, `index.css` перезібрано `bash design-system/components/build.sh` (35 компонентів); desktop screenshot 1531 показав відсутність бокових білих полів, mobile правило не зачіпає. Розсинхрон: значення нового токена не додавалось, правило живе у design-system/components/base.css і автоматично застосовується до всіх `.app--detail`; розмітку компонента не міняли, тому ui/kit.html та ручне перенесення на екрани не потрібні; build.sh виконано. |
| 12:30 | 3хв | `/wireframes/listings.html` | прибери лінію справа (`section.filterbar`) | agent + impeccable | На desktop прибрано `border-right` у спільному правилі `.app--catalog .catalog-workspace > .filterbar` у `design-system/components/feed.css`; sticky-колонка й відступ збережені. **Розсинхрон за CLAUDE.md:** правка живе у спільному компоненті й автоматично охоплює `listings` та всі його стани; токени, розмітку екранів і `ui/kit.html` не чіпали. Правило лишилося тільки в desktop `@media (min-width: 67.5rem)`, а `bash design-system/components/build.sh` виконано, тож `components/index.css` синхронний із джерелом. |
| 12:27 | 11хв | `/wireframes/listings.html` | це постав під 24 оголошення (`fieldset.seg`) | agent + impeccable | Desktop `.catalog-workspace` у спільному `design-system/components/feed.css` отримав grid areas `filters / heading / segment / feed`; `.seg` стоїть під `.catalog-heading`, звичайні `.filters-inline` лишилися sticky зліва; state-main явно в `feed`. Mobile/tablet не зачеплені, бо правило лише в `@media (min-width: 67.5rem)`; попередню правку «без border-right» збережено. Розмітку й токени не чіпали, тому ui/kit і ручне перенесення не потрібні; це спільне правило охоплює всі listings-стани, тож розсинхрону за CLAUDE.md немає. `bash design-system/components/build.sh` виконано; detector `[]`; візуальний browser-pass не запускався за AGENTS.md. |
| 12:36 | 7хв | `/wireframes/listings.html` | просто пошук і поле коротше (input у .tabbar-search) | agent + impeccable | Placeholder скорочено до «Пошук» у `ui/kit.html`, `ui/shell.html` та всіх active/archive wireframes; desktop ширину форми зменшено з 220px до 180px через primitive `--size-180` у `tokens.css`, правило — в `components/tabbar.css`; `DESIGN.md` оновлено, `build.sh` виконано. Розсинхрону немає, бо значення у tokens, правило у спільному компоненті, markup/placeholder перенесено у kit/shell та всі екрани; desktop @media не зачіпає mobile/tablet геометрію, але копірайт placeholder синхронний. |
| 12:39 | 8хв | `/wireframes/listings.html` | на desktop зробити `.card` карткою з відступами й показувати на hover фон зі скругленням | agent + impeccable | У desktop-гілці `shared design-system/components/card.css` `.card` отримав `padding: var(--space-12)` та `border-radius: var(--radius-22)`; hover-фон `--bg-surface` обмежений (`hover:hover`); mobile/tablet не зачеплені. Токени вже існували, розмітка не змінювалась, `ui/kit.html` уже містить `.card`; `build.sh` виконано. Розсинхрону немає, бо це правило спільного компонента, а width-specific виняток лишився в `@media`. |
| 12:42 | 5хв | `/wireframes/listings.html` | `скругдення ховера більщі зроби (.card)` | agent + impeccable | Desktop `.card` radius збільшено з `--radius-22` до наявного `--radius-26` у спільному `design-system/components/card.css`; `focus-visible` синхронізовано; tokens і markup/ui/kit не змінювались; правило лишилось у desktop `@media`, тож mobile/tablet не зачеплені; `build.sh` виконано; розсинхрону за CLAUDE.md немає, бо правило системне й generated `index.css` перебудований. |

| 12:46 | 2хв | `/wireframes/listings.html` | прибери бордер (`.catalog-heading`) | Codex · gpt-5.6-terra · medium | У desktop-правилі спільного каталогу прибрано нижній `border-bottom` із `.catalog-heading`: лічильник «24 оголошення» та активні фільтри тепер розділені тільки простором, без зайвої лінії. Розмітку, токени й mobile/tablet-правила не змінювали; зміна автоматично охоплює listings-стани. `bash design-system/components/build.sh` виконано, generated `index.css` синхронний. |

| 12:48 | 5хв | `/wireframes/listings.html` | це біля фільтра постав | Codex · gpt-5.6-terra · medium | Підсумок активних фільтрів «Поділ · до 10 000 ₴ · спочатку нові» перенесено під кнопкою у `.filters-inline`; у заголовку результатів лишилося «24 оголошення». Для підсумку додано стриманий стиль у shared `feed.css`; `bash design-system/components/build.sh` виконано, `index.css` синхронний. Структурна перевірка й `git diff --check` пройшли. |
| 12:53 | 12хв | `/wireframes/listings.html` | треба на таких речах показувати бредкрамси, на всіх сторінках (`.app-head`) | Codex · gpt-5.6-terra · medium | У шапку додано семантичний `nav.breadcrumbs` із поточною сторінкою до всіх 62 active wireframes та обох прикладів `app-head` у `ui/kit.html` / `ui/shell.html`; shared-стилі додано в `design-system/components/header.css`, а `index.css` перебудовано. Хлібні крихти використовують доступний `aria-label`, список і `aria-current="page"`; лінки мають hover/focus-стани. Статично перевірено покриття всіх 62 app-екранів (`missing: []`), семантичну структуру та `git diff --check`; `bash design-system/components/build.sh` успішний. |
| 14:00 | <1хв | `/wireframes/listings.html` | **rework:** ще чуть більше скруглення (`.card`) | Codex · gpt-5.6-luna · low | У desktop-гілці `.card` та її `focus-visible` скруглення збільшено з `--radius-26` до наявного `--radius-30`; mobile/tablet і `.card-photo` не змінювались. `index.css` перебудовано та перевірено локально. |

| 14:02 | 2хв | `/wireframes/room.html` | треба покращити відступи і композицію сторінки (`main`) | Codex · gpt-5.6-luna · low | У desktop-композиції room detail додано міжрядковий ритм `var(--space-16)` для `main`; правка обмежена `.app--room-detail`, build виконано, selector/output і git diff --check перевірені. |
| 12:59 | 4хв | `/wireframes/listings.html` | тут внизу бордер додай чуть видимий (`.app-head`) | Codex · gpt-5.6-terra · medium | У shared `.app-head` додано тихий нижній роздільник `var(--border-1) solid var(--border-default)`: межа використовує наявний семантичний токен і не змінює структуру чи mobile/desktop-адаптацію. `bash design-system/components/build.sh` виконано, generated `index.css` синхронний; структурна перевірка й `git diff --check` пройшли. |

| 12:59 | 7хв | `/wireframes/listings.html` | додай фон і відступи; фон трохи темніший за поточний (`.filters-inline`) | Codex · gpt-5.6-terra · medium | У desktop-правилі shared `feed.css` sticky-панель фільтрів отримала `--bg-neutral`, внутрішній відступ `--space-16` і скруглення `--radius-22`; семантичні токени дають трохи темнішу поверхню без тіні. Mobile/tablet-правила та розмітку не змінено; `bash design-system/components/build.sh` виконано, `index.css` синхронний. Статично перевірено desktop selector/media-область і `git diff --check`. |
| 14:04 | 3хв | `/wireframes/listings.html` | сегмент праворуч від `24 оголошення` і sticky | Codex · gpt-5.6-terra · medium | На desktop сегмент переміщено праворуч від `24 оголошення` і зроблено sticky у shared `feed.css`; mobile/tablet не зачеплені, `index.css` rebuilt. Без browser/screenshot. |

| 13:05 | 5хв | `/wireframes/listings.html` | зменшити розмір заголовка в компоненті й на всіх desktop-макетах (`.app-head`) | Codex · gpt-5.6-terra · medium | У desktop-гілці shared `design-system/components/header.css` `.head-word` зменшено з наявного `--font-size-40` до `--font-size-28`; правило автоматично діє на всі макети з цим компонентом. Mobile/tablet, розмітку й токени не змінювали; `bash design-system/components/build.sh` виконано, generated `index.css` синхронний. Статично перевірено область desktop-media та selector; detector має лише попередні попередження у `listings.html`, нових для цієї правки немає. |

| 14:12 | 8хв | `/wireframes/listings.html` | зроби варіант карток як таблиця і додай перемикач (`.feed`) | Codex · gpt-5.6-terra · medium | Додано доступний перемикач «картки / таблиця» з `aria-pressed`, видимим focus і збереженням вибору в localStorage. Табличний варіант перетворює ті самі посилання на щільні рядки для порівняння фото, назви, локації та ціни; на desktop ціна займає окрему колонку. Shared `feed.css` змінено й `bash design-system/components/build.sh` виконано, тому `index.css` синхронний. Перевірено наявність токенів, структуру/скрипт і `git diff --check`; browser/screenshot не запускались. |
| 14:14 | <1хв | `/wireframes/listings.html` | менше ширини (`.tabbar-search`) | Codex · gpt-5.6-terra · medium | Desktop `.tabbar-search` уже має 180px (`--size-180`); статична структурна перевірка пройшла. `bash design-system/components/build.sh` виконано, generated `index.css` синхронний; detector unavailable. |
| 14:20 | <1хв | `/wireframes/listings.html` | прибери це тільки для цієї сторінки і тільки для десктопа (`.app-head`) | Codex · gpt-5.6-luna · low | На цій сторінці додано локальне desktop-only правило `.app.app--catalog > .app-head { display: none; }` у `@media (min-width: 67.5rem)`; shared CSS і mobile не змінювались. Статично перевірено селектор і `git diff --check`; build не потрібен. |
| 14:15 | 3хв | `/wireframes/listings.html` | менше ширини (`.tabbar-search`) | Codex · gpt-5.6-terra · medium | Виявлено, що default `min-width: auto` flex-елемента утримував фактичні 225px попри basis 180px. У desktop `.tabbar-search` додано `min-width: 0`, тож intrinsic мінімум більше не блокує `--size-180`; `bash design-system/components/build.sh` успішний, source і generated `index.css` статично синхронні, `git diff --check` пройшов. |
| 13:09 | 4хв | `/wireframes/listings.html` | коли ширина велика то робити по три картки в ряд і так далі (`ul.feed`) | Codex · gpt-5.6-terra · medium | У desktop-оверрайді `.app--catalog .catalog-results .feed` кількість колонок змінено з 2 на 3, тож специфічніше правило більше не перебиває базову 3-колонкову сітку. Mobile/tablet не зачеплені; `bash design-system/components/build.sh` виконано, generated `index.css` синхронний. `detect.mjs` повернув `[]`, `git diff --check` пройшов. |
| 13:09 | 2хв | `/wireframes/listings.html` | без бордера (`form.tabbar-search`) | Codex · gpt-5.6-terra · medium | У desktop-правилі shared `design-system/components/tabbar.css` прибрано border у `.tabbar > .tabbar-search`; скруглення, фон та видимий focus-стан лишилися. Розмітку, токени й mobile/tablet не змінювали; `bash design-system/components/build.sh` успішно синхронізував `index.css`. Перевірено selector у source/bundle, `git diff --check`; Impeccable detector повернув лише попередні warnings/advisories у `listings.html`, не пов’язані з правкою. |
| 14:24 | <1хв | `/wireframes/listings.html` | прибери відступ згори (`ul.feed`) | Codex · gpt-5.6-luna · low | Для цієї сторінки в desktop додано локальний override `.app.app--catalog .catalog-results > .feed { padding-top: 0; }`; shared CSS, інші екрани й mobile/tablet не змінювались. Перевірено структуру селектора та `git diff --check`; build не потрібен. |
| 13:12 | 3хв | `/wireframes/listings.html` | ширше блок (`.filters-inline`) | Codex · gpt-5.6-terra · medium | У desktop-сітці shared `feed.css` ширину колонки sticky-панелі фільтрів збільшено з `16rem` до `18rem`, тож два поля мають достатній простір без зміни внутрішнього ритму. Правило лишається в `@media (min-width: 67.5rem)`: mobile/tablet, розмітку й семантику не зачеплено. `bash design-system/components/build.sh` виконано, `index.css` синхронний; scoped layout scan і `git diff --check` пройшли. |
| 14:30 | 8хв | `/wireframes/applications.html` | треба норм таблицю зробити (`main.operation-main`) | Codex · gpt-5.6-terra · medium | Список заявок перетворено на семантичну таблицю з `caption`, заголовками «Заявка / Стан / Дія» та трьома узгодженими рядками. Додано shared-стилі `operation-table` у `design-system/components/list.css`: тихі роздільники, видимий focus і горизонтальне прокручування на вузьких екранах; `index.css` перебудовано. Статично перевірено структуру таблиці, присутність стилів у bundle та `git diff --check`; browser/screenshot не запускався. |

| 14:31 | <1хв | `/wireframes/listings.html` | full-width table view | Codex · gpt-5.6-terra · medium | Table view розгорнуто на всю ширину: DOM-заголовки «Фото / Оголошення / Район / Перевірка / Ціна» та наявні у картках фото, назва, локація, перевірка й ціна зібрано в щільні колонки; cards view не змінено. |

| 14:40 | 8хв | `/wireframes/listings.html` | картка людини: окремий компонент із круглим аватаром поверх розмитого затемненого фону | Codex · gpt-5.6-terra · medium | Профільні картки позначено `card--person`: незмінний контейнер 4:3 тепер містить круглий аватар, центрований із вертикальними відступами, поверх дубля того ж фото з blur і затемненням. Фонове зображення приховане від assistive technology, змістовний `alt` лишився лише на аватарі. Табличний вигляд зберігає один круглий аватар без фону. Shared `card.css` і `feed.css` оновлено, `bash design-system/components/build.sh` виконано; `git diff --check`, лічильники структури (4/4/4) та синхронність bundle пройшли. Detector має тільки наявні попередження поза цією правкою. |
| 13:25 | <1хв | `/wireframes/listings.html` | фіксуй при скроллі | Codex · gpt-5.6-terra · medium | Виправлено анотацію «фіксуй при скроллі». |

| 13:44 | <1хв | `/wireframes/listings.html` | збільши аватарки, щоби віступ вгорі знизу був 16 пх | Codex · gpt-5.6-terra · medium | Avatar у `person-card` збільшено до 75% ширини контейнера 4:3, тож за наявного `padding-block: var(--space-16)` відступи зверху й знизу становлять 16px. Shared CSS перебудовано; структурну перевірку та `git diff --check` пройдено. |
| 13:47 | <1хв | `/wireframes/listings.html` | на мобілці аватарка просто кружок зробити | Codex · gpt-5.6-terra · medium | На mobile/tablet `person-card` показує лише круглий аватар без розмитого затемненого фону; desktop не змінено, shared CSS rebuilt. |

| 14:45 | 2хв | `/wireframes/listings.html` | бейдж «телефон, фото» не повинен тягнутися фоном | Codex · gpt-5.6-terra · medium | Для фото-варіанта `.badge--on-photo` прибрано успадкований surface-фон; позицію, відступи та маркер верифікації не змінено. |
| 14:50 | <1хв | `/wireframes/listings.html` | аватар: 16px зверху й знизу | Codex · gpt-5.6-terra · medium | На desktop ширина avatar — `calc(75% - 2×16px)`, тож у 4:3 content box із `padding-block: 16px` зверху й знизу рівно 16px; mobile/tablet не змінені. Shared CSS build виконає main agent. |
| 16:14 | 5хв | `/wireframes/listings.html` | зроби білий фон для inline-фільтрів | Codex · gpt-5.6-terra · medium | У desktop-селекторі `.filters-inline` фон змінено з `--bg-neutral` на світлу поверхню `--bg-surface`; збірку shared CSS виконано. |
| 16:21 | 2хв | `/wireframes/listings.html` | **rework:** «верни все назад» (inline-фільтри) | Codex · gpt-5.6-terra · medium | У desktop-селекторі `.filters-inline` повернуто попередній фон `--bg-neutral` замість `--bg-surface`; mobile/tablet та інші state-гілки не змінювалися. Shared `index.css` перебудовано; `git diff --check` і статична перевірка selector/media-області пройшли. |
