{
  "name": "kutok-agentation-build",
  "private": true,
  "version": "0.0.0",
  "description": "Локальна тека для збірки standalone-бандла тулбара Agentation (dev/agentation.js). Не публікується, не входить у git (див. .gitignore: dev/build/node_modules/).",
  "scripts": {
    "build": "bash build.sh"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "agentation": "^3.0.2"
  }
}
